import { SequenceParserKeyPipe } from './sequence-parser-key.pipe';

describe('SequenceParserKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new SequenceParserKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
