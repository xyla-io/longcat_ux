import { RuleDescriptionPipe } from './rule-description.pipe';

describe('RuleDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new RuleDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
