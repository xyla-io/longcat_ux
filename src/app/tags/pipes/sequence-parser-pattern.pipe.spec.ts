import { SequenceParserPatternPipe } from './sequence-parser-pattern.pipe';

describe('SequenceParserPatternPipe', () => {
  it('create an instance', () => {
    const pipe = new SequenceParserPatternPipe();
    expect(pipe).toBeTruthy();
  });
});
