import { EntityDisplayNamePipe } from './entity-display-name.pipe';

describe('EntityDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new EntityDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
